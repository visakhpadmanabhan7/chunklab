# Runtime dependencies (mirrors pyproject.toml [project.dependencies]).
# torch (pulled in by docling) is installed CPU-only in the Dockerfile.

# web
fastapi
uvicorn[standard]
python-multipart>=0.0.26
sse-starlette

# llm + embeddings
groq
openai
anthropic
fastembed>=0.4
transformers>=4.46,<5   # docling 2.x needs the transformers 4.x API (5.x breaks it)
tokenizers
tiktoken

# chunking
langchain
langchain-core
langchain-community
langchain-text-splitters
langchain-experimental
llama-index-core

# parsing
docling
pypdf

# database + vectors
sqlalchemy>=2
asyncpg
pgvector
alembic
greenlet>=3.4.0

# jobs + cache
arq
redis>=5

# rate limiting
slowapi

# config / metrics
pydantic
pydantic-settings
python-dotenv
numpy
